BRMS TABLE Query 1 : 


 CREATE TABLE "SYEDMAAZ"."BRMS" 
   (	"ACCNO" NUMBER(14,0), 
	"NAME" VARCHAR2(30) NOT NULL ENABLE, 
	"EMAIL" VARCHAR2(27), 
	"PHONE_NUMBER" NUMBER, 
	"ACCOUNT_TYPE" VARCHAR2(10), 
	"DEPOSIT" NUMBER(15,0) NOT NULL ENABLE, 
	 PRIMARY KEY ("ACCNO")

LOGIN TABLE QUERY 2 :


 CREATE TABLE "SYEDMAAZ"."LOGIN" 
   (	"USERNAME" VARCHAR2(40), 
	"PASSWORD" VARCHAR2(40) NOT NULL ENABLE, 
	 PRIMARY KEY ("USERNAME")